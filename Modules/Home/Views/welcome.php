<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>IshmaelPHP Starter</title>
    <style>
        html, body { height: 100%; font-family: system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, sans-serif; }
        body { display: grid; place-items: center; margin: 0; background: #0b1021; color: #e6edf3; }
        .card { background: #111936; padding: 2rem 2.5rem; border-radius: 12px; box-shadow: 0 10px 30px rgba(0,0,0,.35); max-width: 640px; }
        h1 { margin: 0 0 0.5rem 0; font-size: 1.75rem; }
        p { line-height: 1.55; color: #c6d0e3; }
        code { background: #0b1021; padding: .2rem .35rem; border-radius: 6px; }
        a { color: #8ad1ff; text-decoration: none; }
        a:hover { text-decoration: underline; }
        .muted { opacity: .75; }
    </style>
    </head>
<body>
    <main class="card">
        <h1>Welcome to IshmaelPHP Starter</h1>
        <p>You should see this page after installing dependencies and serving the <code>public/</code> directory.</p>
        <p>There is also a tiny JSON endpoint at <a href="/home/api"><code>/home/api</code></a>.</p>
        <p class="muted">Docs are available inside <code>Docs/</code> once you copy Core docs using the included script.</p>
    </main>
</body>
</html>
